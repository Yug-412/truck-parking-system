# Server environment variables example
# Port the server will listen on (Render will provide PORT automatically)
PORT=5000

# Frontend URL to allow CORS from (set to your Render frontend URL)
FRONTEND_URL=https://YOUR_FRONTEND.onrender.com

# (Optional) If you want to override database file location
# DATABASE_FILE=database/truckParking.db
